# Hlekkr Platform Configuration
# Copy this file to .env.local and configure for your environment

# Demo Mode (secure default - no API access)
REACT_APP_DEMO_MODE=true

# Production API Configuration
# These values are intentionally blank for security
# Production deployments must provide these via environment variables
REACT_APP_API_BASE_URL=
REACT_APP_API_KEY=

# AWS Region (if using real API)
REACT_APP_AWS_REGION=us-east-1

# Demo Configuration (safe to be public)
REACT_APP_DEMO_TRUST_SCORE_IMAGE=85
REACT_APP_DEMO_TRUST_SCORE_VIDEO=32
REACT_APP_DEMO_TRUST_SCORE_AUDIO=45

# Security Note:
# - No production API endpoints are included in this repository
# - Demo mode is the secure default for all users
# - Production access requires explicit API key provisioning